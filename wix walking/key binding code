
local function onKeyEvent(event)
	for event.keyName == "a"  do	
		--insert what you want to move--.x = {-0.5}
	for event.keyName == "w" do
		--insert what you want to move--.y = {-0.5}	
	for event.keyName == "d"  do	
		--insert what you want to move--.x = {0.5}
	for event.keyName == "s" do
		--insert what you want to move--.y = {0.5}	
	end
end


 
Runtime:addEventListener ("key", onKeyEvent)

--note this is very basic and also needs a loop but it was the most i could figure out for now--